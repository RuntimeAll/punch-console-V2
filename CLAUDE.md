# CLAUDE.md — ai-bkb-v2 知识工厂（总图）

> **v2 = 独立生态的知识库制造工厂**（2026-08-16 立项，08-18 设计全定稿转入分批开发）。
> 🔴 **第一原则：独立生态零交互**——不调备课帮 prod 任何服务（book-server/teacher-mcp/101 教务链），
> 不读写老区 `D:\workplace\ai-bkb` 的库与运行态（老区**只读**，资产平移非引用）。飞书是通道不在隔离范围。
> 题目数据与备课帮**后期可互通**（交换格式 `question-exchange/v1`，非合库）。
> 需求 SSOT=[认知/需求总纲.md](认知/需求总纲.md)；结构 SSOT=[认知/数据结构.md](认知/数据结构.md)。

## 0. 四大关键使用场景（一切围绕它们组织）

| # | 场景 | 流程正本 |
|---|---|---|
| 1 | **每日打卡** | 母题定性→初稿确认→铺开 20/30 天（DSL 出题路径必留档）→发布 |
| 2 | **举一反三** | 认母题→深解析→算子调配（认识懂变·变一变二）→生成变体，全程血缘入库 |
| 3 | **录题**（讲义/试卷/好题） | 相似前查→模板→切割→抽离带账→等级审（L0~L3，拍照+1）→执行阀五条→入库 |
| 4 | **批改流水线** | 收作业→确定考点（认卷）→批改核对→总结归纳→出报告；九态状态机；**自包含可迁云（101）** |

全流程细则=[认知/业务流程.md](认知/业务流程.md)。不在这四条里的能力一律先不建。

## 1. 目录结构（2026-08-18 定版，改拓扑先改本表）

| 目录 | 是什么 | 纪律 |
|---|---|---|
| `认知/` | 🔴 SSOT hub：需求总纲/数据结构/架构总图/业务流程/迁移蓝图 + `考古/`（老区考证件，只查不写） | 方向有变先改正本 |
| `记录/` | 流水账：迁移记录.md + 各批次变更记录 | 没记账的动作=没发生 |
| `prd/` | PRD 卡（v2 独立计数，调度中心发号） | 卡自包含，独立 session 可执行 |
| `知识库/` | 运行态①：kb.db + 资产/（图等二进制，库存指针） | 🔴 只在主位，不进 git |
| `批改产线/` | 运行态②：grading.db/收件箱/题单快照/学员/ ——**自包含可迁云单元** | 同上；与知识库只有两根管道 |
| `产物/` | 产线出品成品（册子/卷/报告） | artifact 指针指这里，全相对路径 |
| `测试数据/` | 语料（从老区按需拷入） | 只进不引用 |
| `console/` | 展示台代码（React19+antd5+Vite，:4300） | 挂账+展示+终审，页面只读写归 agent |
| `工具箱/` | 生产工具代码：切割器/渲染链/DSL/验算闸/KG 工具 | 代码空间只放代码 |
| `.claude/skills/` | v2 skill hub（编制表=其 README） | 每次执行落 skill_log（D-15） |
| `punch-console/` | 发布物料中控台（:3000，Next.js+SQLite）——🔴 **独立 git 仓**（origin=RuntimeAll/punch-console.git），整体平移自老区、不重构 | 库=`知识库/资料库.db`（PUNCH_DB env）；起法见 §5；不并入 v2 仓 |
| `试验场/` | POC/战役目录（带日期） | 用完挪归档 |
| `开发位/` | git worktree 停车场 | 见 §2 |
| `password/` | 凭据 | 🔴 永不入 git |
| `归档/` | 完结战役收纳 | 只查不写 |

## 2. 🔴 开发模式：单仓 + worktree + 主位常驻运行

- **主位（本目录）= 唯一运行位**：master 分支；:4300 展示台、watcher、两库、产物都只活在主位。
- **并行开发按需开位**：`git worktree add 开发位\wt-<线名> -b dev/<线名>`——一个独立 session 领一个位；
  完成合回 master 即收位 `git worktree remove`。不养空位。
- **三条硬纪律**：
  1. worktree 里**绝不碰主位的两库**（SQLite 单写者，双写必坏账）；动库结构在自己位里用**副本库**沙盘，
     DDL 定稿后回主位一次显式执行；
  2. **公共契约件只在主位改**（认知/数据结构.md、DDL、console 的 mock/types）——要动先到调度中心排队；
  3. **不合回 master 不算完结**（PRD 完结判据含"已合并+已收位"）；小步快合，不养长命分支。
- **巡检口诀**（每批次开工前）：`git branch --no-merged master`（欠账清单）+ `git worktree list` + 各位 `git status`。
- 分支流：master 唯一主线；开工从 master 拉分支；边写边提交（中文 message）。

## 3. 数据纪律（每一条都有老区血案，别复验）

- 🔴 **id 全链路字符串**（雪花 JSON double 截尾会建空壳卷）；**文件指针全相对路径**（老货架 717 行绝对路径全断）。
- 🔴 **存取同构**：题目正文只有一份 blocks_json（md+$LaTeX$，块级 role），存什么用什么，无出库转换层。
- 🔴 **一切靠闸不靠注释**（老区三处"注释谎言"实锤）：块流校验器/考点叶子闸/执行阀五条/守恒闸——违例拒收不静默。
- 🔴 **凡"拍平"操作必配守恒闸**；凡不确定判定标低置信进人审，绝不静默。
- 🔴 **术语禁"台账"**（用 记录/清单/待办/队列）；字段单名制（同物两名=静默丢的坑）。
- 🔴 学情=按轨现算不落表；分值/题号不进 question 表；小问不拆（一标记一题）。
- 🔴 py 脚本开头 `sys.stdout.reconfigure(encoding='utf-8')`（GBK 控制台 emoji 杀进程）；
  跨库搬数据 pymysql/py 纯通路（PowerShell 重定向毁中文）；PowerShell 复杂命令落脚本再跑。

## 4. 红线

- 老区与备课帮 prod **零变更**（隔离原则的必然）；凭据只落 `password/`。
- 对外报告：肯定判断禁模糊词；总结简洁禁铺陈。
- （学员用代号是**现状事实不是限制**——本来就没有真名可记；"代号制/不触达"类隐私条款已全局删除，2026-08-18 用户拍板，别再往回加。）

## 5. 常用命令

```powershell
# 展示台（主位常驻）
cd D:\workplace\ai-bkb-v2\console; pnpm exec vite --port 4300 --strictPort --host 127.0.0.1
# 🔴 必须显式 --host 127.0.0.1（默认只绑 ::1 连不上）；build 自证 = pnpm build（tsc -b && vite build）
# 探活（本机有代理必须 --noproxy）
curl.exe -s -o NUL -w "%{http_code}" --noproxy "*" http://127.0.0.1:4300/
# 发布物料中控台 :3000（punch-console）——🔴 pnpm dev 会被依赖体检拦（better-sqlite3 装失败遗留），用 node 直启：
#   cd punch-console\web; $env:PUNCH_DB='D:\workplace\ai-bkb-v2\知识库\资料库.db'; node node_modules\next\dist\bin\next dev
# Chrome 截图/出 PDF（撞正开着的 Chrome 会静默不写文件）
# --headless=new --disable-gpu --no-proxy-server --user-data-dir=<临时profile>，截完 sleep 再验文件存在
```

## 6. 批次现状（详=[认知/迁移蓝图.md](认知/迁移蓝图.md)，流水=[记录/迁移记录.md](记录/迁移记录.md)）

① 地基（目录/git/两库 DDL/三闸/判据首批导入）→ ② 批改产线实装 → ③ 录入线+KG 建树 →
④ 生产线六 skill → ⑤ 展示台接真库 → ⑥ 后置（OCR 图片链/互通导出/MathType 债/飞书）。

## 7. 独立 session 开工模板

```
你在 D:\workplace\ai-bkb-v2 领一张卡独立完成。先读根 CLAUDE.md 与卡（prd/PRD-NNN-*/PRD.md），照卡执行不越界。
开工先领 worktree：git worktree add 开发位\wt-<线名> -b dev/<线名>，只在自己位里干活；
主位两库与运行态绝不碰；完成合回 master 并收位才算完结；拿不准的标低置信写进报告，不停下等人。
```
