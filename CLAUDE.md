# CLAUDE.md — ai-bkb-v2 知识工厂（总图）

> **v2 = 独立生态的知识库制造工厂**（2026-08-16 立项，08-18 设计全定稿转入分批开发）。
> 🔴 **第一原则：独立生态零交互**——不调备课帮 prod 任何服务（book-server/teacher-mcp/101 教务链），
> 不读写老区 `D:\workplace\ai-bkb` 的库与运行态（老区**只读**，资产平移非引用）。飞书是通道不在隔离范围。
> 题目数据与备课帮**后期可互通**（交换格式 `question-exchange/v1`，非合库）。
> 需求 SSOT=[认知/需求总纲.md](认知/需求总纲.md)；结构 SSOT=[认知/数据结构.md](认知/数据结构.md)。
> 🔴 **三正本同级**（2026-08-19 用户立）：[北极星](认知/北极星.md)（为什么/往哪去）｜架构与数据结构（是什么）
> ｜[开发规范](认知/开发规范.md)（怎么建）——立卡先对北极星，感到漂移回北极星对表。

## 0. 四大关键使用场景（一切围绕它们组织）

| # | 场景 | 流程正本 |
|---|---|---|
| 1 | **每日打卡** | 母题定性→初稿确认→铺开 20/30 天（DSL 出题路径必留档）→发布 |
| 2 | **举一反三** | 认母题→深解析→算子调配（认识懂变·变一变二）→生成变体，全程血缘入库 |
| 3 | **录题**（讲义/试卷/好题） | 相似前查→模板→切割→抽离带账→等级审（L0~L3，拍照+1）→执行阀五条→入库 |
| 4 | **批改流水线** | 收作业→确定考点（认卷）→批改核对→总结归纳→出报告；九态状态机；**自包含可迁云（101）** |

全流程细则=[认知/业务流程.md](认知/业务流程.md)。不在这四条里的能力一律先不建。

## 1. 目录结构（2026-08-18 定版，改拓扑先改本表）

| 目录 | 是什么 | 纪律 |
|---|---|---|
| `认知/` | 🔴 SSOT hub：需求总纲/数据结构/架构总图/业务流程/迁移蓝图 + `考古/`（老区考证件，只查不写） | 方向有变先改正本 |
| `记录/` | 流水账：迁移记录.md + 各批次变更记录 | 没记账的动作=没发生 |
| `prd/` | PRD 卡（v2 独立计数，调度中心发号） | 卡自包含，独立 session 可执行 |
| `知识库/` | 运行态①：kb.db + 资产/（图等二进制，库存指针） | 🔴 只在主位，不进 git |
| `批改产线/` | 运行态②：grading.db/收件箱/题单快照/学员/ ——**自包含可迁云单元** | 同上；与知识库只有两根管道 |
| `成品库/` | 🔴 **唯一成品存放地**（2026-08-22 用户归一令「所有成品只走一个文件夹·隔离清楚」）：artifact.files_json 只许指这里；一册一目录 `<artifact_id>·<人话名>/`；**只放挂账成品**，中间件/过程件/镜像一律不得入内 | 不进 git；挂账即归库（artifact_tool 闸强制拷入换指针）；:4300 成品速览页直读直预览 |
| `产物/` | 工艺过程与验收件（渲染出件的自由区，含 _源 中间件） | 不再被 files 指；挂账时由闸拷入 成品库/ |
| `测试数据/` | 语料（从老区按需拷入） | 只进不引用 |
| `console/` | 展示台代码（React19+antd5+Vite，:4300） | 挂账+展示+终审，页面只读写归 agent |
| `工具箱/` | 生产工具代码：切割器/渲染链/DSL/验算闸/KG 工具 | 代码空间只放代码 |
| `.claude/skills/` | v2 skill hub（编制表=其 README） | 每次执行落 skill_log（D-15） |
| `punch-console/` | 🔴 **代码冻结·转任只读手机展示台**（2026-08-20 用户改判，取代"待退役"）：:3000 用 next start 起服，数据=`知识库/资料库-投影.db`（工具箱/桥/投影同步.py 从 kb.db 单向重建，冻结的 资料库.db 仍是历史存档只读）；桌面工厂控制台=:4300，资料/物料/发布展示（含手机）=:3000 | 代码/仓零变更（仅 web/.env.local 配置）；手机上改人工态会被同步覆盖，真改动走 :4300/agent |
| `备课/` | 🔴 **备课线工作区**（2026-08-22 用户令进 v2，蓝图「备课链不迁」作废）：一学生一批次目录，内含 `课程计划.md`（正本，契约 course-plan/v1）+ 每课次目录（备课单/双卷 PDF）；老区 420 文件已整体平移（SHA256 零差异） | 不入 git；🔴 代码只在 `工具箱/备课/`，动前先跑 `体检.py`（拦 prod 直连与老区写路径） |
| `试验场/` | POC/战役目录（带日期） | 用完挪归档 |
| `codeplace/` | git worktree 停车场（2026-08-19 用户定名，替代旧 `开发位/`） | 见 §2 |
| `password/` | 凭据 | 🔴 永不入 git |
| `归档/` | 完结战役收纳 | 只查不写 |

## 2. 🔴 开发模式：单仓 + worktree + 主位常驻运行

- **主位（本目录）= 唯一运行位**：master 分支；:4300 展示台、watcher、两库、产物都只活在主位。
- **并行开发按需开位**：`git worktree add codeplace\wt-<线名> -b dev/<线名>`——一个独立 session 领一个位；
  完成合回 master 即收位 `git worktree remove`。不养空位。
- **三条硬纪律**：
  1. worktree 里**绝不碰主位的两库**（SQLite 单写者，双写必坏账）；动库结构在自己位里用**副本库**沙盘，
     DDL 定稿后回主位一次显式执行；
  2. **公共契约件只在主位改**（认知/数据结构.md、DDL、console 的 mock/types）——要动先到调度中心排队；
  3. **不合回 master 不算完结**（PRD 完结判据含"已合并+已收位"）；小步快合，不养长命分支。
- **巡检口诀**（每批次开工前）：`git branch --no-merged master`（欠账清单）+ `git worktree list` + 各位 `git status`。
- 🔴 **多 session 并行先读 [记录/开发注册.md](记录/开发注册.md)**（2026-08-19 建制）：开工登记领地、
  两库写窗口排队、公共件排队、资源占用照实更新——没登记的占用=违例。
- 分支流：master 唯一主线；开工从 master 拉分支；边写边提交（中文 message）。
- 🔴 **subagent 模型口径**（2026-08-20 用户令）：缺省 **opus**；简单活（检索/格式化/搬运/清点）走
  **sonnet**，禁全员 opus；**fable 仅限主循环，subagent 一律禁用**——Agent/Workflow 派工显式传
  model，别靠继承漏成 fable。

## 3. 数据纪律（每一条都有老区血案，别复验）

- 🔴 **id 全链路字符串**（雪花 JSON double 截尾会建空壳卷）；**文件指针全相对路径**（老货架 717 行绝对路径全断）。
- 🔴 **存取同构**：题目正文只有一份 blocks_json（md+$LaTeX$，块级 role），存什么用什么，无出库转换层。
- 🔴 **一切靠闸不靠注释**（老区三处"注释谎言"实锤）：块流校验器/考点叶子闸/执行阀五条/守恒闸——违例拒收不静默。
- 🔴 **凡"拍平"操作必配守恒闸**；凡不确定判定标低置信进人审，绝不静默。
- 🔴 **术语禁"台账"**（用 记录/清单/待办/队列）；字段单名制（同物两名=静默丢的坑）。
- 🔴 学情=按轨现算不落表；分值/题号不进 question 表；小问不拆（一标记一题）。
- 🔴 py 脚本开头 `sys.stdout.reconfigure(encoding='utf-8')`（GBK 控制台 emoji 杀进程）；
  跨库搬数据 pymysql/py 纯通路（PowerShell 重定向毁中文）；PowerShell 复杂命令落脚本再跑。

## 4. 红线

- 老区与备课帮 prod **零变更**（隔离原则的必然）；凭据只落 `password/`。
- 对外报告：肯定判断禁模糊词；总结简洁禁铺陈。
- （学员用代号是**现状事实不是限制**——本来就没有真名可记；"代号制/不触达"类隐私条款已全局删除，2026-08-18 用户拍板，别再往回加。）

## 5. 常用命令

```powershell
# 展示台（主位常驻）
cd D:\workplace\ai-bkb-v2\console; pnpm exec vite --port 4300 --strictPort --host 127.0.0.1
# 🔴 必须显式 --host 127.0.0.1（默认只绑 ::1 连不上）；build 自证 = pnpm build（tsc -b && vite build）
# 探活（本机有代理必须 --noproxy）
curl.exe -s -o NUL -w "%{http_code}" --noproxy "*" http://127.0.0.1:4300/
# 读 API + 语意 serve（常驻一对，启动台幂等管）：python 工具箱\启动台.py（--status 只探活 / --stop 全停）
# 手机展示台（punch-console 投影，2026-08-20 改判转任）：数据重建 python 工具箱\桥\投影同步.py；
# 起服=node 直启（serve-v2.ps1 会触发 pnpm 依赖体检→gyp 编译必败，别用）：
#   cd punch-console\web; node node_modules\next\dist\bin\next start -H 0.0.0.0 -p 3000
# kb.db 变了不自动跟：重跑投影脚本+重启 :3000；冻结的 资料库.db 仍只读存档
# Chrome 截图/出 PDF（撞正开着的 Chrome 会静默不写文件）
# --headless=new --disable-gpu --no-proxy-server --user-data-dir=<临时profile>，截完 sleep 再验文件存在
```

## 6. 批次现状（详=[认知/迁移蓝图.md](认知/迁移蓝图.md)，流水=[记录/迁移记录.md](记录/迁移记录.md)）

> 🔴 2026-08-20 本节按 [迁移蓝图](认知/迁移蓝图.md) 二次重排改写（原写的旧批次序=重排前口径，已作废）。

**已收口**：① 地基（目录/git/两库 DDL/三闸/判据 134 条）✅ → ② 生产线（KG 工具+回流通路+六 skill
+库中心组卷渲染+D-20 检索三层+D-21 等级审）✅ → PRD-003 资料库合并吃库（账层全吃、题层试刀待放量，
挂起五项见卡 §6）🚧 → ③ **收尾战役 2026-08-20**（PRD-008 ①②③：出卷流水线转正（draft_paper/
paper_run/answer_batch/出卷流水线 skill/三配方 template，金标 219/221）+四闸补缺（asset-add 写入门/
X7 悬空闸/泄答案闸白名单/管线契约，套件 193 绿）+卷7卷8 43 题实弹入库带答案+工单代审 37/38+
promote 1089/1090 上架+账面清整）✅——PRD-008 ④发布链适配留卡。

**2026-08-21 追加收口**：④ **展示台真库转正**（真库页覆盖正式路由 /questions /artifacts /papers
/kg /model /criteria /templates，mock 收「待办·未去mock」组）✅ → ⑤ **窗L·KG 题型层**（kp 六层树
102→197、挂载闸拆双道「就近可挂/存量合法」、取题整枝、第6章 58 题下沉 26，守恒八项 PASS）✅
＋映射总口径入 数据结构 §2.2b（过目正本）＋认知盘账（两草案归档 归档/认知草案/）✅
＋考点打卡出件器 render_punch.py（sync_kaodian 首启，样稿被打回、模版线打磨中）
→ ⑥ **窗N/窗O 小学枝四上 KG**（kp 197→332：版本「小学数学」通壳+四年级上册，
🔴 窗N 按教辅册铺被用户打回，窗O 改**教材同步结构**重铺＝9 单元 14 小节 110 考点，
章节源=26秋新版人教教材实翻、考点源=老区讲义解析账；「目录壳=教材章节不是教辅册」
+小学不分教材版本两口径入 §2.2b）✅。

**2026-08-22 实践驱动连发六窗（用户令「以实践驱动开发」「在一遍遍使用中补足」）**：
⑦ **备课线进 v2**（🔴 蓝图「备课链留老区」改判）：老区备课产物 420 件平移 `备课/`（SHA256 零差异）
+四年级/一年级备课 skill 去 teacher-mcp 改造（计划载体=`plan_tool.py` 读 `课程计划.md`，
两起真实事故做成撞主题闸/改判留痕闸）+学情落 grading.db（student_tool 肖像七键）✅
→ ⑧ **窗Q 四上期末全流程实战**（4 张拍照 → Workflow 9 opus 转录/双盲/裁判 → 26 题入库
+3 图重画 → 系统出卷双 PDF；顺带补老版四上 3 单元 kp 332→351）✅
→ ⑨ **窗S 考频分析沉淀**（`卷面考频分析.py`：任意卷→考频报告+exam_model 首个「怎么造卷」型；
单卷口径=主挂+0.5×次挂，🔴 不冒充全局 kp.freq）✅
→ ⑩ **窗T 答案卷定版**（`--answers-compact` 纯答案：只留题号+答案，解答题列算式链）✅
→ ⑪ **窗U 成品库归一 + 成品速览页**（🔴 用户令「所有成品只走一个文件夹」：顶层 `成品库/` 立根、
792 件迁入、**挂账即归库**闸；`/deliverables` 页 793 件点行直接预览，读 API 18→20 端点）✅
→ ⑫ **窗V 成品件硬绑**（用户令「关联关系补上去」：新表 `artifact_file`＝册外键+role 八值
+sha256+卷血缘，793 件回填；`files-list --role/--dup` 按角色取件与查重；files_json 降为兼容视图）✅
＋网盘账对账闸（`网盘账对账.py`：总表 × 库逐条比链/提取码/网盘路径）✅

**当前双主线（调度中心）**：**PRD-009 KG 本体弹性完备**（映射表/考点向量建叶闸/三原语/翻译清账，
~2.5 天，层名改判挂用户闸）｜**PRD-010 七上打卡产出线**（重要级队列七件已排，铺册等模版定稿）。
另：七下 KG 平移 + PRD-003 题层放量（用户闸）仍在队；🔴 **四上/五上题层与一年级枝**是备课线
实际用得到却尚缺的（四上 KG 有枝 26 题、五上无枝只能出件不入库）。

**并行待领**（各领一卡一 worktree）：PRD-004 举一反三（🔴 底层件窗L 后全就位，只剩编排 skill
~1.5 天）｜PRD-005 批改产线（学情源已实证机器可读，前菜「学情→专项」可先交付）｜PRD-006 录入线
｜PRD-007 展示台余量（门禁四盏灯+发布台跳转链后期件）｜PRD-008 ④发布链适配。
PRD-001/002 已完结（002 于 2026-08-21 补记收口）。

**后置**：OCR 图片链/question-exchange 互通导出/MathType 债/飞书通道（批改常驻飞书方向）。

## 7. 独立 session 开工模板

```
你在 D:\workplace\ai-bkb-v2 领一张卡独立完成。先读根 CLAUDE.md 与卡（prd/PRD-NNN-*/PRD.md），照卡执行不越界。
开工先领 worktree：git worktree add codeplace\wt-<线名> -b dev/<线名>，只在自己位里干活；
主位两库与运行态绝不碰；完成合回 master 并收位才算完结；拿不准的标低置信写进报告，不停下等人。
```
