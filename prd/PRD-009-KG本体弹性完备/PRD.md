# PRD-009 · KG 本体弹性完备（映射表 · 考点向量准入闸 · 生命周期原语）

> 立卡：2026-08-21（调度中心发号）｜状态：📋 可开工（设计全定稿，用户已确认"可以摊开"）
> 设计正本＝本目录 [设计正本-KG弹性机制.md](设计正本-KG弹性机制.md)（七节全案）＋
> [设计正本-KG加层策略.md](设计正本-KG加层策略.md)；落库口径正本＝认知/数据结构.md §2.2b。
> 🔴 已落库的前半（勿重做）：题型层（kp 197 节点）/ 双闸分家（assert_leaf_kp 新挂载·就近可挂
> ＋assert_mounted_kp 存量）/ 取题整枝（query_core CTE）/ 第6章 58 题下沉 26——窗L 2026-08-21，
> 守恒八项 PASS，回归四套件绿。本卡＝**设计里剩余未施工的全部机器件**。

## 0. 一句话

把"教材切换零重挂、考点准入有闸、树改得起"从设计变成机器——本卡收口后，
KG 弹性机制设计.md 里的每一条承诺都有对应的表/闸/命令，不再有"设计定稿未施工"字样。

## 1. 铁律

- 🔴 开工先到 记录/开发注册.md 登记；kb.db 写全部走主位写窗口，agent 一律沙盘副本；
- 🔴 凡动树/挂载必配守恒对账（行数+逐行哈希），凡不确定低置信人审，绝不静默；
- 🔴 存量 71 考点的现名现 id 零改动（改名裁决除外，见段1）；主位执行前 kb.db.bak；
- 模型口径：开发 opus / 清点 sonnet / fable 不下场。

## 2. 分段（可独立收口，按序）

### 段1 层名改判执行（👤 用户闸先裁）
用户疑义（2026-08-21）：现层名"考点(71)/题型(95)"与其口径"知识点下挂考点"相反，且"题型"与
qtype 形态义撞词。**裁决项**：A=改名（71→'知识点'、95→'考点'，DDL CHECK 重建+六处代码/文档扫换，
~1h 沙盘先行）；B=维持现名（把词义澄清写死进 §2.2b）。🔴 裁了才动，别抢跑。

### 段2 目录↔本体映射表（教材切换能力的前置，~1 天）
- 建 `kp_map(tree_node_id, kp_id, ord)`；存量 71 考点在浙教2024 树落第一份映射；
- 读路径切换：draft_paper 单元维度（唯一 id 前缀聚合处）、console 树 API、coverage 对账
  改走映射表/CTE；🔴 从此全库禁 id 前缀聚合（数字前缀取题的对外行为不变）；
- 验收＝改版剧本演练（设计正本 §五）：假想新版本树+映射，question_kp 改动行数=0。

### 段3 考点向量 + 建叶闸 + 聚簇巡检（准入标准机器化，~0.5 天）
- `kp_vec` 表；文本向量🔴**必须带全路径**（年级-学期-章/单元-小节-节点名[+desc] 拼串再 embed，
  用户定的准入标准）＋挂载题质心向量两路；
- 建叶前置闸进 kg_tool add-leaf：近邻 top1 超阈值拦下给候选（"已有近似考点 X 带路径"），
  低置信人审；聚簇巡检子命令出并叶/升维工单；
- 判词（用户原话）："确认没有近似值非常高的才能新增，不然都挂一起"。

### 段4 新 id 规则 + 三原语 + 容量三闸（~1 天）
- 新本体节点 id 不编位置（段2 落地后生效；存量 15 位过渡 id 冻结不再新增）；
- kg_tool 拆叶/并叶/挪枝三子命令（退役不删、successor 转发、别名自动铸、skill_log+迁移记录）；
- 容量闸：叶>150 题出拆叶工单 / 0 题叶巡检报表 / anchor 低置信复审队列。

### 段5 翻译层清账（~0.5 天，可与段3 并行）
- 七码错因别名 6 条补铸（符号处理/运算顺序/绝对值脱号/分数小数/运算律简算/括号嵌套——
  场景B 学情翻译的缺口，实测只有"乘方"翻得出）；
- 88 条指向已细分父叶的 kp_alias 改指题型（考点）节点；5 片同名题型叶的择型口径落档；
- 语意近似前查进录题闸（🔴 对题**提示不硬拒**——近似可能正是变体，确认变体带血缘入、重题拒）。

### 段6 桥判据适配（与 PRD-003 挂起#6 同一件，做完对方销账）
bridge_questions/考点映射表 命中判据从旧叶子闸改 assert_mounted 语义＋细分叶择型口径
（老册题无题型标注：挂题型人工择型 or 批量留层例外——👤 随 PRD-003 题层放量一并裁）。

### 段7（后置可选）批量线重挂
讲义 641 管线重跑落题型 / DSL 60 重生成 / 老册 228 留层——就近可挂保证不清也不错，闲时清。

## 3. 工期与验收

段2+3+4+5 ≈ **2~2.5 天**（段1 裁 A 加 ~1h）。总验收＝设计正本 §五 改版剧本全程走通
＋建叶闸正反例＋三原语各一次实操带守恒对账＋场景B 七码翻译全通。

## 4. 开工提示词（独立 session 用）

```
你在 D:\workplace\ai-bkb-v2 领 PRD-009 独立完成。先读根 CLAUDE.md、记录/开发注册.md（🔴 先登记）、
本卡与两份设计正本、认知/数据结构.md §2.2b。段1 用户闸未裁前跳过段1。
开工：git worktree add codeplace\wt-prd009 -b dev/prd009-kg弹性，只在自己位干活；
一切库操作打沙盘副本，主位 kb.db 落库交调度中心写窗口执行；每段守恒对账+正反例全绿才算段收口。
拿不准的低置信写报告，不停下等人。
```
