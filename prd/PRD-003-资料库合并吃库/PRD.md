# PRD-003 · 资料库合并吃库（punch-console 并入 v2 · 首刀只吃最简单可重入）

> 立卡：2026-08-19（调度中心发号）｜状态：📋 待用户过闸
> 🔴 动因（用户拍板链）：①"直接合并到当前服务里面来……资料库和这里的题目库本身就是两者冲突的点，需要合并处理；
> 能否吃下那边的资料合并到一起来，这个是工作量的关键" ②"先录入最简单的，可重入的，其他的放入待办里面来，
> 不急着全入，我们一步步做完" ③开工四条件：每阶段列事项+准入闸必须和用户过一遍／数据重入零 token 确认／
> 不碰源服务、开发在 `codeplace\`／前端样式调整必须先过用户一轮。

## 0. 一句话

把 `知识库\资料库.db`（punch-console :3000 的库）里**最简单、可机械重入**的资产吃进 kb.db——
82 册账 + 2948 道带答案的题——其余全部落待办，一步步做完；页面归一到 :4300。

## 1. 铁律（每条都是闸，不是注释）

- 🔴 **源服务零变更**：punch-console 代码/仓/`资料库.db` 一个字节不改；桥只读 `mode=ro` 打开资料库.db，
  写只进 kb.db。老区文件只拷出（资产平移非引用）。
- 🔴 **数据迁移零 LLM 零 token**：桥全是机械脚本（字段映射+格式包装）；考点映射=一次性对照表+人审清单，
  绝不逐题 AI 打标（2026-08-19 用户红线："不然损耗太大了，还不如直接重做"）。
- 🔴 **桥必幂等（可重入）**：按 hash_L1/match_key 排重，重跑不重复入库、不覆盖人工改动。
- 🔴 开发位 = `D:\workplace\ai-bkb-v2\codeplace\`（用户指定，2026-08-19），worktree 开在这下面；
  主位两库纪律照 CLAUDE.md §2。
- 🔴 前端样式调整必须先出样张过用户一轮才动真页面。
- 🔴 页面只读原则不破：并入后唯一写端点=「人工态」一列（punch-console 血案注释原样搬入：能发≠已发）。

## 2. 范围

**本卡做**：正本+物料域 DDL → 册子账桥（82 doc + 24 合刊 + 49 物料 + 735 PDF 平移）→
题目桥（2948 题 answer 非空的机械入库）→ 前端最小并入（/kb/materials + 人工态端点）→
常驻 embed serve + 收尾冻结。

**本卡不做（全在 §4 待办）**：三升四 620 题（无答案）、A1 剩余 11 本 DSL 重放、A2 三本出题器题型本地重建、
科学线 6 本、C 类无源 200 题、四上单元过关铺量、:3000 正式退役声明。

## 3. 阶段与准入闸（👤=用户闸 ⚙️=机器闸；准入闸不过不开工）

### 阶段0 正本与地基（半天）
做：CLAUDE.md 目录表（+`codeplace\` 条目；punch-console 条目改"冻结只读参考·待退役"）；
数据结构.md 增物料域正本（material/publish_log，宣发字段口径统一）；DDL 副本库沙盘→主位一次显式执行；
`codeplace\` 领 worktree。
- 👤 本卡阶段与准入闸清单用户已过一遍点头（开工闸）
- ⚙️ git 三查清账：`branch --no-merged master` 空 / `worktree list` 无欠位 / `status` 净

### 阶段1 册子账桥（1 天）
做：桥脚本 `工具箱\桥\` —— doc 82→kb.artifact（网盘链接/提取码/在售态/版本组进宣发字段，
kb 侧记 punch_doc_id 映射）；doc_member 24→合刊关系；material 49→物料表；
asset 735 个 PDF 从老区拷入 `产物\历史打卡\`（平移非引用）挂 artifact files **相对路径**。
- ⚙️ 阶段0 DDL 已主位执行、正本已改
- ⚙️ 735 条 PDF 路径存在性**预检报告先出**（缺失列清单不静默，缺不拦整批）
- ⚙️ 幂等自证：同脚本连跑两遍，第二遍零新增零变更

### 阶段2 题目桥（1~1.5 天）
做：2948 题（answer 非空）机械转 blocks_json（md+$LaTeX$ 包装）→ 过三闸+等级审（纯文本 L0）→
草稿态入库，来源串进血缘；考点映射：152 个文字名先机械对 KG/kp_alias，未命中的题照常入库标
「考点待挂·低置信」落人审清单；入库后增量向量化。三升四 620 题无答案→待办不入。
- 👤 考点映射对照表（152 名，大头=六上分数 MOLD 108 名）用户过目
- ⚙️ 副本库沙盘全绿（含反例：无答案拒入、坏块拒入、重跑幂等）
- ⚙️ 入库后抽 20 题在 :4300 页面渲染目检（公式真渲、答案不泄）

### 阶段3 前端最小并入（1~1.5 天）
做：:4310 扩物料端点；/kb/materials 页（A/B 账号面板+一键复制+burned 态）；
/kb/artifacts 册详情补网盘链接/提取码/在售态；「人工态」窄写端点（唯一写，血案注释随行）。
- 👤 🔴 样式方案样张（静态 mock/截图）先过用户一轮才写真页面
- ⚙️ mock 页零改动、页面只读复查（全站写端点=1 个）
- ⚙️ pnpm build 绿 + 截图目检

### 阶段4 常驻 serve 与收尾（半天）
做：embed serve 常驻进程（预载 bge，query_core 探活优先走 serve、失败回落冷载）；
启动台脚本（:4310+serve 同起同灭）；ingest/dsl_batch 收尾自动增量向量；
资料库.db question 表冻结为历史存档声明（文档层，不动库）；迁移记录落账；合回 master 收位。
- ⚙️ 全链冒烟：find_questions 三层/页面两页+物料页/人工态端点
- ⚙️ 存量五套测试+桥测试全绿
- 👤 终验走查（含待办清单确认）

## 4. 待办清单（本卡不做，逐项可单独立卡/领办）

1. **三升四每日一练 ×2 版 620 题**：资料库里无答案（出题器反抽只有题面）——随 A2 本地重建一并补
2. **A1 剩余 11 本 DSL 重放**（qbank 平移+GEN_META 注册表→dsl_batch 零 LLM，每本约半天）：
   七上实数的运算/实数计算/整式化简求值/整式的加减/绝对值压轴、五上同步小纸条/字母与竖式/计算强化、
   五升六暑假、二上表内乘除法（脚本在册根+random seed=天号）、七上有理数混合运算（重建版只出新题，
   且依赖老区 err_kp.json 需先斩依赖）
3. **A2 三本本地重建**：三上混合运算特训、三升四基础/提高——计算模块全走老区 :9190 出题器
   （🔴 红线禁调源服务），multable/divtable/paren 等题型需补本地 DSL 才能续造
4. **科学线 6 本**（长度/体积/温度/实验探究/实验室+测量合刊）：题面硬编码 f-string+SVG 配图，
   import 源码机械抽取+配图平移；「打卡五」无 verify 需补
5. **七上代数式的值 55 题**：qbank 硬编码可机械抽（教辅 39+平行 16，验算件在老区解题模型库）
6. **C 类无源 2 册 200 题**（有理数简便运算/混合运算老成品）：只挂成品账，题目层不做
   （索引红字：别拿重建 DSL 复现老册；要做=OCR 另立卡）
7. **群打卡第01期**：_源 只有 HTML 无 py，缓办
8. **四上单元过关册型铺量**：零产题题源引用型，原料≈100+ SKU，另立卡
9. **:3000 punch-console 正式退役声明**：阶段3 验收后补一条记录+CLAUDE.md 目录表收尾
10. **历史册载体重建（doc×day → paper/paper_item）**：B 题层桥一度顺手建了卷，2026-08-19 审查
    裁决**整件剥离挪待办**——本卡口径只有「吃题」，建卷是另一件设计活，机械桥答不出下面四问，
    答不出就不许写（写了就是假账）。原料已备齐：每题 `prov_json` 带全
    `punch_doc/punch_day/punch_section/punch_seq`，重建时不必回源库重查。立卡时先答这四问：
    ① `layout_json` 缺 `sections` —— 渲染链按 sections 排版，缺了必炸，sections 从哪来（源库 doc 有没有、还是重新定义）；
    ② **部分吃进的卷不能盖「定稿」** —— 2948 题里现只吃进 120，一天 12 题的卷可能只落位几题，
       盖定稿=假账；到底是先建「草稿」卷、还是等该册题吃全了再建；
    ③ **铺枝后重跑会往已定稿的卷里静默追加** —— 那是改稿不是幂等，得先定「卷定稿后还能不能补题」；
    ④ **绕过了 `工具箱/组卷/paper_tool.py` 这道组卷闸**，且历史册本来就存在同题跨天重复，
       与组卷查重闸正面冲突 —— 是让历史册走豁免、还是把闸改成认「历史重建」这一档。

## 5. 进度

- [ ] 0 正本与地基
- [ ] 1 册子账桥（82 册+24 合刊+49 物料+735 PDF）
- [ ] 2 题目桥（2948 题+考点映射表+人审清单）
- [ ] 3 前端最小并入（样张先过用户）
- [ ] 4 常驻 serve 与收尾
