# 待入队列 · 规则未覆盖（PRD-003 题层桥）

> 生成：2026-08-19 18:38:58（工具箱/桥/bridge_questions.py 自动出件，改数据请改源头重跑）

共 **9** 题。判据=块流转换规则表（bridge_questions.py `RULE_TABLE` / `classify`）覆盖不到。
🔴 这些题**没入库**，也**没猜**——补完规则（或补 figure/asset 通路）后重跑桥即自动吃进。

## 按 册×题型×原因 聚合

| 册 | 名称 | 题型 | 原因 | 题数 | 样例 punch_id |
|---|---|---|---|---:|---|
| 4010 | 六上分数乘除法打卡 | fill | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren'] | 5 | 106641 106642 106663 |
| 4010 | 六上分数乘除法打卡 | word_multi | 题面形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren'] | 4 | 106639 106640 106785 |

## 逐题清单

| punch_id | 册 | day | section | 题面形 | 答案形 | 原因 | 题面原文（前 60 字） |
|---|---:|---:|---|---|---|---|---|
| 106639 | 4010 | 2 | 看图列式 | html | paren | 题面形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；题面转换失败：形 'html' 没有确定性变换（不猜） | `一条公路全长 \(450\) 米，下图中涂色部分是已经修好的一段。<div style="margin:1.6mm 0 ` |
| 106640 | 4010 | 2 | 看图列式 | html | paren | 题面形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；题面转换失败：形 'html' 没有确定性变换（不猜） | `粮店运来黄豆和绿豆，数量如下图（每一小段一样长）。<div style="margin:1.6mm 0 1mm"><sv` |
| 106641 | 4010 | 2 | 看清单位“1”，填一填 | paren | html | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；答案转换失败：形 'html' 没有确定性变换（不猜） | `六(2)班有男生 \(36\) 人，男生人数的 \(\frac{5}{9}\) 是女生人数。把（　　）看作单位“1”，女` |
| 106642 | 4010 | 2 | 看清单位“1”，填一填 | paren | html | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；答案转换失败：形 'html' 没有确定性变换（不猜） | `一条彩带长 \(\frac{9}{10}\) 米，剪下它的 \(\frac{2}{3}\)。把（　　）看作单位“1”，剪` |
| 106663 | 4010 | 3 | 想一想，填一填 | paren | html | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；答案转换失败：形 'html' 没有确定性变换（不猜） | `\(\frac{5}{9}\times\frac{3}{4}\times 9=\frac{5}{9}\times 9\t` |
| 106664 | 4010 | 3 | 想一想，填一填 | paren | html | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；答案转换失败：形 'html' 没有确定性变换（不猜） | `\(\frac{7}{8}\times\left(\frac{4}{5}\times 8\right)=\left(\f` |
| 106665 | 4010 | 3 | 想一想，填一填 | paren | html | 答案形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；答案转换失败：形 'html' 没有确定性变换（不猜） | `\(\left(\frac{1}{3}+\frac{5}{6}\right)\times 18=\frac{1}{3}\` |
| 106785 | 4010 | 8 | 列式计算 | html | paren | 题面形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；题面转换失败：形 'html' 没有确定性变换（不猜） | `下图表示一根钢管，每一段同样长。这根钢管全长多少米？<svg width="336" height="56" viewB` |
| 106786 | 4010 | 8 | 列式计算 | html | paren | 题面形=html（含 HTML/SVG 标签 —— 规则不覆盖（配图应走 figure 块+asset，本卡不做））不在本册本题型允许集 ['paren']；题面转换失败：形 'html' 没有确定性变换（不猜） | `下图中甲、乙两筐苹果的每一份同样重。乙筐苹果有多少千克？<svg width="336" height="62" vie` |
